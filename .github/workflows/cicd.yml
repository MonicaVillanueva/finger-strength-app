name: Unit Tests

on:
  push:
    branches:
      - master
      - feature/*
  pull_request:
    branches: [master]

jobs:
  Jest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Run Tests
        run: |
          cd FingerStrength
          npm install 
          npm test